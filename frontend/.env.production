# Production API base URL.
# The backend Express server serves both /api/* and /session-logger/* on the
# same origin, so a relative path works fine. Override with an absolute URL
# if the API is hosted separately.
VITE_API_BASE_URL=https://buildwithkulshresth.com/api
